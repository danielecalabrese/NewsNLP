[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "newsnlp"
version = "0.1.0"
description = "News NLP pipeline"
requires-python = ">=3.14"

[tool.setuptools.packages.find]
where = ["src"]